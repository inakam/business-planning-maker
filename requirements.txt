# Business Planning Maker - Requirements

# Core dependencies (optional, for API server)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Note: The core system works without these dependencies using only the Python standard library
# These are only required for the API server and web UI:
# pip install -r requirements.txt
